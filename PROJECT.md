# PROJECT

## 프로젝트명
DK Logo Editor

## 목적
기존 로고를 기반으로 배경 처리와 부기명 추가를 간단하게 수행하는 Windows 데스크톱 프로그램을 만든다.

## 제품 방향

메인 창은 작업에 필요한 항목만 노출한다.

- 원본 이미지 선택
- 배경 설정
- 부기명 입력
- 출력 크기
- 모델 선택
- 생성/편집
- 결과 저장

API Key, 기본 모델, 커스텀 모델 관리는 별도 설정 창에서 수행한다.

## 로고 보호 정책

원본 소스의 픽셀 크기와 화면상 위치는 고정하지 않는다. 부기명을 배치할 공간을 확보하기 위해 원본 로고를 축소하거나 이동할 수 있다.

단, 다음은 허용하지 않는다.

- 로고의 글자/심볼 형태 변경
- 로고 내부 색상 변경
- 로고 요소 추가/삭제
- 생성형 AI에 의한 로고 재해석 또는 재그리기

즉, 원본 로고의 `scale/position` 변경은 허용하지만 `visual identity` 변경은 금지한다.

## 처리 원칙

1. 원본에서 보호할 로고 영역과 변경 가능한 배경 영역을 구분한다.
2. AI가 필요한 분석/생성 작업은 OpenRouter를 통해 수행한다.
3. AI 결과에서 원본 로고 자체는 최종 결과에 사용하지 않는다.
4. 원본 로고 레이어를 로컬에서 최종 합성한다.
5. 배경색/투명도와 최종 출력 크기는 로컬에서 정확히 적용한다.

## 1차 범위

- PNG/JPG 입력
- 배경 자동 감지
- 배경색 변경
- 투명 배경
- 부기명 텍스트 입력
- AI 기반 부기명 위치/스타일 결정 및 생성
- 원본 로고 보호 합성
- 출력 크기 지정, 기본 200 × 60
- OpenRouter API Key
- 기본 모델 프리셋
- 커스텀 모델 추가/삭제
- 결과 미리보기
- PNG 저장

## 1차 범위 제외

아래 기능은 요구사항이 추가되기 전까지 구현하지 않는다.

- 일괄 처리
- 편집 히스토리/Undo
- 템플릿 시스템
- SVG 벡터 변환
- 클라우드 저장
- 계정/로그인
