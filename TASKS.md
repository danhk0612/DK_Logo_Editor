# TASKS

작업은 아래 순서대로 진행한다. 완료 조건을 만족하기 전에는 다음 단계의 핵심 기능을 완료 처리하지 않는다.

## T01. 프로젝트 골격 및 기본 창

- [x] .NET 8 WPF 프로젝트 생성
- [x] MainWindow 생성
- [x] SettingsWindow 생성
- [x] 메인 창에 최소 작업 항목 배치
- [x] 기본 출력 크기 200 × 60 표시
- [x] 프로젝트가 Windows에서 빌드 가능한 구조인지 확인

완료 조건: GitHub Actions Windows/.NET 8 Release 빌드에서 Restore와 Build 성공.

## T02. 설정 저장 및 모델 목록

- [x] AppSettings 정의
- [x] OpenRouter API Key 저장/로드
- [x] 기본 모델 프리셋 정의
- [x] 기본 모델 선택 저장
- [x] 커스텀 model slug 추가/삭제
- [x] 메인 창 모델 셀렉터와 연결
- [x] 마지막 제작 설정 저장/복원 구조 추가
- [x] 실제 종료 → 재실행 후 제작 설정/원본 경로 복원 확인

사용자 로컬 실행 검증 완료.

## T03. OpenRouter 클라이언트

- [x] OpenRouter HTTP 클라이언트 구현
- [x] 이미지 입력 전달
- [x] 선택 모델 호출
- [x] 응답 처리
- [x] 로고/부기명 분석용 요청 계약 정의
- [x] 메인 생성 흐름에 OpenRouter 호출 연결
- [x] 실제 OpenRouter API Key와 원본 이미지로 편집 요청/응답 확인

사용자 로컬 생성 결과 확인으로 실제 API 호출 경로 검증 완료.

## T04. 로고/배경 분리

- [x] 배경 후보 자동 분석
- [x] 배경 마스크 생성
- [x] 보호 로고 레이어 추출
- [x] 지정 배경색 적용 기반 구현
- [x] 투명 배경 적용 기반 구현
- [x] 배경 연결 영역 판정 허용 오차 보강
- [x] AI 부기명 영역의 배경 제거 후 투명 레이어화 구현
- [ ] 현대제철 등 실제 로고 샘플로 수정 후 배경 분리 품질 재확인

구현 방식: 외곽과 연결된 배경만 제거하여 로고 내부의 동일색 픽셀은 보호하고, AI 부기명 결과도 별도 배경 제거 후 합성한다.

완료 조건: AI 호출 없이 원본 로고를 보호한 채 배경만 변경할 수 있다.

## T05. 캔버스 레이아웃

- [x] 출력 크기 적용
- [x] 원본 로고 종횡비 유지 리사이즈
- [x] 로고가 출력 영역을 벗어나지 않도록 배치
- [x] AI가 로고 축소 여부와 배치 영역을 판단하는 레이아웃 플래너 구현
- [x] AI가 부기명 위치와 정렬을 고정 하단 규칙 없이 판단하도록 구현
- [x] AI 계획을 정규화 좌표 → 안전한 실제 좌표로 변환
- [x] 로고/부기명 겹침 방지 및 실패 시 fallback 구현
- [ ] 실제 여러 비율의 로고/출력 크기로 AI 레이아웃 품질 확인

레이아웃 AI: OpenRouter 비전 호출로 원본 로고, 부기명, 최종 출력 크기를 보고 로고 영역/부기명 영역/정렬을 결정한다. 프로그램은 로고 종횡비와 보호 규칙을 강제한다.

완료 조건: 고정 배치가 아니라 로고와 출력 비율에 맞춰 자연스럽게 재배치할 수 있다.

## 추가 UI 요구사항

- [x] API Key 표시/숨김 눈동자 버튼
- [x] TextBox/PasswordBox 세로 가운데 정렬
- [x] 원본 이미지 선택 및 마지막 원본 경로 복원
- [x] 원본/결과 마우스 휠 줌
- [x] 원본/결과 드래그 이동
- [x] 두 미리보기의 줌/이동 동기화
- [x] 결과 확정 시 원본 폴더 자동 저장 공통 경로 구현
- [x] 기존 PNG 저장 버튼을 다른 이름으로 저장 용도로 변경
- [x] 위 항목 사용자 로컬 실행 확인
- [x] 배경색 직접 입력 + 마우스 색상 선택기 구현
- [x] 색상 입력값과 선택기 실시간 동기화 구현
- [x] 투명 배경 선택 시 색상 입력/선택 UI 비활성화
- [ ] 색상 선택기/투명 모드 사용자 로컬 실행 확인

## T06. AI 기반 부기명

- [x] 원본 로고와 출력 비율을 OpenRouter 레이아웃 판단에 전달
- [x] AI가 로고 크기/위치 및 부기명 위치/정렬을 자동 판단
- [x] 판단 결과에 따라 보호 로고를 먼저 배치한 AI 입력 이미지 구성
- [x] 최대 4배 작업 해상도에서 부기명 생성 후 최종 크기로 축소
- [x] AI 편집 범위를 계획된 부기명 영역으로 제한하는 요청 계약 구현
- [x] 패널/띠/박스 생성 금지 프롬프트 강화
- [x] AI 결과 중 부기명 영역만 추출하고 배경 제거 후 최종 합성
- [x] 최종 단계에서 보호 원본 로고를 다시 합성
- [ ] 현대제철 + 당진공장 샘플로 수정 결과 확인
- [ ] 가로가 긴/짧은 출력에서 AI가 서로 다른 자연스러운 배치를 선택하는지 확인
- [ ] 투명 배경 + 부기명 결과의 글자 가장자리/알파 품질 확인

완료 조건: 부기명을 입력하면 원본 로고를 변경하지 않고 AI가 자연스러운 전체 구성을 판단하여 결과를 생성한다.

## T07. 전체 편집 흐름 연결

- [x] 이미지 선택 → 설정 → 생성/편집 연결
- [x] 원본/결과 미리보기
- [x] 생성 성공 즉시 원본 폴더 자동 PNG 저장 연결
- [x] 다른 이름으로 PNG 저장
- [x] 투명 배경 알파 유지 기반 구현
- [x] 생성 중 버튼 잠금/상태 표시
- [x] 기존 전체 생성 흐름 사용자 로컬 확인
- [ ] 새 AI 레이아웃/부기명 후처리를 포함한 전체 흐름 재확인

완료 조건: 메인 창에서 한 번의 작업 흐름으로 결과 PNG를 저장할 수 있다.

## T08. 검증 및 배포 준비

- [ ] 대표 로고 샘플 검증
- [ ] 원본 로고 변형 여부 비교
- [ ] 배경 변경 검증
- [ ] 부기명 포함/미포함 검증
- [x] 현재 코드 Release 빌드 검증

완료 조건: 1차 버전 요구사항이 모두 검증되고 배포 가능한 Release 빌드가 생성된다.
