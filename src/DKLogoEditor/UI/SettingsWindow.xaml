<Window x:Class="DKLogoEditor.UI.SettingsWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="설정 - DK Logo Editor"
        Width="520"
        Height="460"
        ResizeMode="NoResize"
        WindowStartupLocation="CenterOwner"
        ShowInTaskbar="False">
    <Grid Margin="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" Margin="0,0,0,18">
            <TextBlock Text="OpenRouter API Key" FontWeight="SemiBold" Margin="0,0,0,6" />
            <PasswordBox Height="30" />
        </StackPanel>

        <StackPanel Grid.Row="1" Margin="0,0,0,18">
            <TextBlock Text="기본 모델" FontWeight="SemiBold" Margin="0,0,0,6" />
            <ComboBox Height="30" IsEnabled="False">
                <ComboBoxItem Content="T02에서 모델 목록 연결" IsSelected="True" />
            </ComboBox>
        </StackPanel>

        <StackPanel Grid.Row="2" Margin="0,0,0,10">
            <TextBlock Text="커스텀 모델" FontWeight="SemiBold" Margin="0,0,0,6" />
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="8" />
                    <ColumnDefinition Width="72" />
                </Grid.ColumnDefinitions>
                <TextBox Grid.Column="0" Height="30" IsEnabled="False" />
                <Button Grid.Column="2" Content="추가" Height="30" IsEnabled="False" />
            </Grid>
        </StackPanel>

        <Border Grid.Row="3" BorderBrush="#D0D0D0" BorderThickness="1" CornerRadius="4" Margin="0,0,0,18">
            <TextBlock Text="T02에서 커스텀 모델 목록을 연결합니다."
                       Foreground="#777777"
                       HorizontalAlignment="Center"
                       VerticalAlignment="Center" />
        </Border>

        <Button Grid.Row="4"
                Content="닫기"
                Width="90"
                Height="34"
                HorizontalAlignment="Right"
                Click="CloseButton_Click" />
    </Grid>
</Window>
