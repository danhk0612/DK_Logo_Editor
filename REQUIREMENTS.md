# REQUIREMENTS

## R01. 원본 로고 입력

- 사용자는 PNG 또는 JPG/JPEG 로고 이미지를 선택할 수 있어야 한다.
- 선택한 이미지를 메인 창에서 미리 볼 수 있어야 한다.
- 원본 이미지의 해상도는 최종 출력 해상도와 같을 필요가 없다.

## R02. 원본 로고 보호

- 배경이 아닌 원본 로고 요소는 생성형 AI가 변경해서는 안 된다.
- 로고의 형태, 색상, 내부 요소를 유지해야 한다.
- 부기명 공간 확보를 위한 로고 전체의 축소 및 위치 이동은 허용한다.
- 최종 합성 시 보호 대상 로고는 원본에서 가져와야 한다.

## R03. 배경 처리

사용자는 다음 중 하나를 선택할 수 있어야 한다.

- 지정 색상 배경
- 투명 배경

배경 변경은 판정된 배경 영역에만 적용해야 한다.

## R04. 부기명

- 사용자는 추가 텍스트(부기명)를 입력할 수 있어야 한다.
- 부기명이 비어 있으면 추가하지 않는다.
- 부기명의 위치, 크기, 정렬, 간격, 시각적 스타일은 원본 로고와 출력 비율을 고려해 자연스럽게 구성한다.
- AI가 필요한 부기명 분석/생성은 OpenRouter를 사용한다.
- 부기명 공간 확보를 위해 원본 로고 크기와 위치를 조정할 수 있다.

## R05. 출력 크기

- 가로와 세로를 각각 픽셀 단위로 지정할 수 있어야 한다.
- 기본값은 200 × 60 px이다.
- 최종 결과는 지정한 정확한 크기로 출력해야 한다.

## R06. OpenRouter

- AI가 필요한 모든 외부 모델 호출은 OpenRouter를 통해 수행한다.
- API Key는 설정 창에서 입력한다.
- 기본 모델을 선택할 수 있어야 한다.
- 프로그램이 제공하는 기본 모델 프리셋이 있어야 한다.
- 사용자가 OpenRouter model slug를 직접 입력해 커스텀 모델을 추가할 수 있어야 한다.
- 추가한 커스텀 모델을 삭제할 수 있어야 한다.

## R07. 모델 선택

- 메인 창에서 현재 작업에 사용할 모델을 선택할 수 있어야 한다.
- 설정 창에서 지정한 기본 모델을 최초 선택값으로 사용한다.
- 기본 프리셋과 사용자 커스텀 모델을 하나의 선택 목록에서 사용할 수 있어야 한다.

## R08. 설정 창

메인 창과 분리된 설정 창에서 다음을 관리한다.

- OpenRouter API Key
- 기본 모델
- 커스텀 모델 추가
- 커스텀 모델 삭제

## R09. 메인 창

메인 창은 최대한 단순하게 구성한다.

필수 요소:

- 원본 이미지 선택
- 원본/작업 미리보기
- 배경 설정
- 부기명 입력
- 가로/세로 출력 크기
- 모델 선택
- 생성/편집 실행
- 결과 미리보기
- PNG 저장
- 설정 버튼

## R10. 결과 저장

- 1차 버전의 결과 저장 형식은 PNG로 한다.
- 투명 배경 선택 시 알파 채널을 유지한다.

## R11. 로컬 이미지 처리

정확성과 원본 보호를 위해 다음 작업은 로컬에서 수행할 수 있다.

- 배경 마스크 적용
- 원본 로고 보호 레이어 합성
- 캔버스 구성
- 이미지 리사이즈
- 알파 처리
- 최종 PNG 인코딩

이 작업들은 생성형 AI 작업으로 간주하지 않으며 OpenRouter 사용을 요구하지 않는다.
